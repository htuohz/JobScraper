import React from "react";
import TopLocations from "../components/TopLocations";

const TopLocationsPage: React.FC = () => {
  return (
      <div style={{ marginTop: "2rem" }}>
        <TopLocations />
      </div>
  );
};

export default TopLocationsPage;
